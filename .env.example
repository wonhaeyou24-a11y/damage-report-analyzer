# STEP 12-1 — 개발환경에서 서버 측(백엔드)이 사용할 Gemini API Key.
# 실제 키를 이 파일에 쓰지 말 것. `.env` 또는 `.env.local`로 복사한 뒤 값을 채워서 사용한다.
# (사용자가 화면의 "AI 설정"에서 직접 Gemini API Key를 입력하는 경우, 이 값은 없어도 된다 —
#  화면에서 입력한 키가 우선 사용되고, 이 환경변수는 그 값이 없을 때만 fallback으로 쓰인다.)
GEMINI_API_KEY=
